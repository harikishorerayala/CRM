import { HighliteTextDirective } from './highlite-text.directive';

describe('HighliteTextDirective', () => {
  it('should create an instance', () => {
    const directive = new HighliteTextDirective();
    expect(directive).toBeTruthy();
  });
});
